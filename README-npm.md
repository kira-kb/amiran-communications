# Amiran Communications Website

A modern, beautiful website for Amiran Communications built with Next.js 15, TypeScript, and Tailwind CSS.

## 🚀 Getting Started with npm

### Prerequisites

````markdown
# Amiran Communications — npm instructions

This file contains quick npm-based instructions for local development and deployment.

## Prerequisites

- Node.js 18+ and npm

## Install & run

```bash
npm install
npm run dev
```
````

Build and run production:

```bash
npm run build
npm run start
```

## Scripts (package.json)

- `dev` — start dev server (port 3000)
- `build` — create production build
- `start` — run production server
- `lint` — run ESLint

## Notes

- Favicon & site icons are served from `public/logo.png`.
- Metadata is in `src/app/layout.tsx` and updated to match https://baltoncp.com/amirancomm/.
- The navbar highlights the active page (desktop & mobile).

## Security

- Dependencies were audited after updates; no critical vulnerabilities remain locally.

If you prefer `pnpm` or `bun`, I can provide equivalent commands.

```

```
